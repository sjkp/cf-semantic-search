{
  "name": "cf-semantic-search",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npx wrangler deploy",
    "create-index": "npx wrangler vectorize create sjkpdk --dimensions=768 --metric=cosine"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.37.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.1.0",
    "@qdrant/js-client-rest": "^1.8.1"
  }
}
